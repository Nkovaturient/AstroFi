const config = {
  plugins: ["@tailwindcss/postcss", "autoprefixer"],
  tailwindcss: {
    config: "./tailwind.config.js",
  },
  autoprefixer: {
    // Add any specific options for autoprefixer here
  },
};

export default config;
